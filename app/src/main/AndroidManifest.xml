<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampUs">
        <activity
            android:name=".MainFeedsActivity"
            android:exported="false"
            android:label="@string/title_activity_main_feeds" />
        <activity
            android:name=".SocialFeed"
            android:exported="false"
            android:label="@string/title_activity_social_feed"
            android:theme="@style/Theme.CampUs.NoActionBar" />
        <activity
            android:name=".CreateProfile"
            android:exported="false" />
        <activity
            android:name=".SignIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>